import cp.

main => 
    Weights = [2, 3, 4, 5, 9, 7],
    Values  = [3, 4, 5, 8, 10, 6],
    Capacity = 15,
    Take = new_list(6),
    Take :: 0..1,
    TotalWeight #= sum([T*W : [T,W] in zip(Take, Weights)]),
    TotalValue #= sum([T*V : [T,V] in zip(Take, Values)]),
    TotalWeight #=< Capacity,
    solve([$max(TotalValue)], [Take]),
    println([TotalValue, Take]).


